[options]
Architecture = auto
DBPath = /custom/lib/pacman/
RootDir = /custom/root/
SigLevel = Required DatabaseOptional

[core]
Include = /etc/pacman.d/mirrorlist

[extra]
Include = /etc/pacman.d/mirrorlist

Include = /etc/pacman.d/local-repos.conf
